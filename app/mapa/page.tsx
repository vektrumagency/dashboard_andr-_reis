import { ComingSoon } from "@/app/components/ComingSoon";

export default function MapaPage() {
  return (
    <ComingSoon
      title="Mapa"
      description="Pins dos imóveis no mapa, coloridos por prioridade. Próximo na ordem de construção — falta ainda geocodificação no backend (lat/lng)."
    />
  );
}
